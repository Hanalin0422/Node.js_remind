<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/main.css">
</head>
</head>
<body class="grey-bg">

    <%- include('nav.ejs') %>

    <div class="detail-bg">
        <h4><%= title%></h4>
        <p><%= content%></p>

        <hr style="margin-top:60px">
        
        <% if(comment != null){ %>
            <% comment.forEach(function(e, i){ %>
                <div>
                    <p><strong><%= comment[i].writer%></strong> <%= comment[i].content %> </p>
                </div>
            <% }) %>
        <% } %>

        <form action="/comment" method="POST">
            <input type="text" name="content">
            <input name="parentID" value="<%= id %>" style="display : none">
            <button type="submit">댓글작성</button>
        </form>

        <button class="amend" onclick="location.href='/detail/<%= id %>/modify'">수정하기</button>
    </div> 

    <button onClick="location.href='/list'">뒤로 돌아가기</button>
</body>
</html>